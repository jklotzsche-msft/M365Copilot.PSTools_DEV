@{
    RequiredPermission = 'Teams admin', 'Global admin'
    Commands           = @{
        'Get-CsTeamsMeetingPolicy' = @{
            M365CopilotRelation = 'The CsTeamsMeetingPolicy cmdlets enable administrators to control the type of meetings that users can create or the features that they can access while in a meeting. It also helps determine how meetings deal with anonymous or external users. You can use the Teams admin center or PowerShell to manage how users in your org use Copilot in Teams meetings and events.'
            M365CopilotLink     = 'https://learn.microsoft.com/en-us/microsoftteams/copilot-teams-transcription#manage-copilot-using-powershell'
        }
        'Set-CsTeamsMeetingPolicy' = @{
            M365CopilotRelation = 'The CsTeamsMeetingPolicy cmdlets enable administrators to control the type of meetings that users can create or the features that they can access while in a meeting. It also helps determine how meetings deal with anonymous or external users. You can use the Teams admin center or PowerShell to manage how users in your org use Copilot in Teams meetings and events.'
            M365CopilotLink     = 'https://learn.microsoft.com/en-us/microsoftteams/copilot-teams-transcription#manage-copilot-using-powershell'
        }
    }
}